export type sliderTypes = {
    id: number,
    name?: string,
    enName?: string,
    img: string,
    light?: boolean,
}

export type sliderItemType = {
  id: number,
  name?: string,
  img: string,
}
