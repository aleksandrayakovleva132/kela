export type catalogTypes = {
  index?: number,
  image?: string,
  titleRu?: string,
  titleEn?: string,
  date?: string,
  long?: boolean,
  images?: object,
  descriptionRu?: object,
  descriptionEn?: object,
  open?: boolean,
}
