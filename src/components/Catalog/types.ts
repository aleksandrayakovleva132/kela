export type catalogTypes = {
  index?: number,
  image?: string,
  titleRu?: string,
  titleEn?: string,
  date?: string,
  long?: boolean,
  images?: object,
  bigImage?: string,
  descriptionRu?: object,
  descriptionEn?: object,
  open?: boolean,
}
