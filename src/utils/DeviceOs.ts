// eslint-disable-next-line no-shadow
enum DeviceOs {
  ANDROID = 'android',
  WINDOWS = 'windows',
  IOS = 'ios',
  MACOS = 'macos',
  LINUX = 'linux',
  OTHER = 'other',
}

export default DeviceOs;
